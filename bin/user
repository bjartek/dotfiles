#!/bin/bash
if [ "$#" -lt 1 ]; then
  echo "Spefisier IDENT"
  exit 1
fi
ldapsearch -Q -H "ldap://ldap.skead.no:3268" -b "DC=skead,DC=no" "(&(objectCategory=person)(objectClass=user)(sAMAccountName=$1))"

#ldapsearch -x -h skead.no -D "${USER}@skead.no" -W -b "ou=konsulenter,ou=skatteetaten,dc=skead,dc=no" -s sub "(cn=*)" cn mail sn
